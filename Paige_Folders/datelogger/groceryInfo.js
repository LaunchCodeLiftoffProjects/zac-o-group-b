//this is where the class, objects, and array of objects are stored as variables to be used in script.js 

//dates from user input need to be split at '-' and replaced with ',' to be read by the date function and trip the alert 

class GroceryItem {
  constructor(name, expires,) { ... }
}
